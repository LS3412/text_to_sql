fastapi>=0.100.0
uvicorn>=0.22.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
asyncpg>=0.28.0
redis>=5.0.0
python-json-logger>=2.0.0
clickhouse-connect>=0.6.0
llama-index-core>=0.10.0
llama-index-llms-ollama>=0.1.0
# Pipeline additions:
sqlglot>=25.0          # stages 6/8: AST parse/validate + dialect transpile (soft: degrades to regex)
PyYAML>=6.0            # stage 2: semantic data dictionary (soft: degrades to MINIMAL_DICTIONARY)
llama-index-embeddings-ollama>=0.3  # stage 3 RAG embeddings (soft: degrades to keyword-only)
pgvector>=0.3         # stage 3 vector index helpers
pg8000>=1.30.0        # pure-Python sync driver (schema reflection + embedding indexer)
locust>=2.15.0